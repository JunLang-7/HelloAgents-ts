# Minimal dependencies for fixture generation (offline, no API keys).
# Install: uv venv .venv --python 3.12 && VIRTUAL_ENV=.venv uv pip install -r requirements.txt
pydantic>=2.0,<3
numpy>=2.0,<3
scikit-learn>=1.0
qdrant-client>=1.9.0,<1.16.0
neo4j>=5.0
openai>=1.0,<2
requests>=2.25,<3
python-dotenv>=0.19,<2
beautifulsoup4>=4.9,<5
networkx>=2.6,<4
tiktoken>=0.5
